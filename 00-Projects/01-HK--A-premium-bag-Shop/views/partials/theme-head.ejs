<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<style type="text/tailwindcss">
    @theme {
        --font-sans: "Inter", sans-serif;
        --color-primary: #DB4444;
        --color-primary-hover: #E07575;
        --color-night: #000000;
        --color-light-gray: #F5F5F5;
        --color-text-gray: #7D8184;
    }
    @layer base {
        html {
            scroll-behavior: smooth;
        }
        body {
            background-color: #FFFFFF;
            color: #000000;
            font-family: var(--font-sans);
        }
        ::selection {
            background: rgba(219, 68, 68, 0.24);
        }
    }
    @layer components {
        .hk-input {
            @apply w-full border-b border-stone-300 bg-transparent px-0 py-3 text-sm text-stone-900 outline-none transition duration-200 placeholder:text-stone-400 focus:border-primary;
        }
        .hk-btn {
            @apply inline-flex items-center justify-center gap-2 rounded bg-primary px-10 py-4 text-sm font-medium text-white transition duration-300 hover:bg-primary-hover;
        }
        .hk-btn-outline {
            @apply inline-flex items-center justify-center gap-2 rounded border border-stone-400 bg-white px-10 py-4 text-sm font-medium text-stone-900 transition duration-300 hover:border-black;
        }
    }
</style>
