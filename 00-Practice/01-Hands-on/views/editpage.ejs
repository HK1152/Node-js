<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white font-sans p-10">
    <div class="main max-w-4xl mx-auto">
        <a href="/file/<%= title %>" class="text-blue-400 hover:text-blue-300 text-sm font-medium">Back</a>
        <div class="form bg-zinc-800 p-8 rounded-xl border border-zinc-700 shadow-lg mt-5">
            <h2 class="text-2xl font-bold mb-6 text-zinc-100">Edit Task</h2>
            <form action="/edit/<%= title %>" method="post" class="flex flex-col gap-4">
                <input type="text" name="newTitle" value="<%= title %>" placeholder="Title..."
                    class="bg-zinc-700 border border-zinc-600 rounded-md px-4 py-2 text-white outline-none focus:border-zinc-500">
                <textarea name="newDescription" placeholder="Description..." rows="3"
                    class="bg-zinc-700 border border-zinc-600 rounded-md px-4 py-2 text-white outline-none focus:border-zinc-500 resize-none"><%= description %></textarea>
                <input type="submit" value="<%= submitText %>"
                    class="bg-blue-600 hover:bg-blue-700 transition-colors text-white font-semibold py-2 rounded-md cursor-pointer">
            </form>
        </div>
    </div>
</body>

</html>